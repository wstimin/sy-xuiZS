# 1Panel 专属部署说明

本文档用于通过 1Panel 网页安装、配置、更新和迁移 X-UI 搭建助手。安装过程不需要登录服务器终端，不需要手工执行 Docker 命令，也不需要在宿主机安装 Node.js、npm 或 PM2。

## 下载地址

- 永久最新版安装包：[xui-deploy-assistant-1panel.zip](https://github.com/wstimin/sy-xuiZS/releases/download/1panel-latest/xui-deploy-assistant-1panel.zip)
- 永久发布页面：[1panel-latest](https://github.com/wstimin/sy-xuiZS/releases/tag/1panel-latest)
- SHA256 校验文件：[SHA256SUMS](https://github.com/wstimin/sy-xuiZS/releases/download/1panel-latest/SHA256SUMS)

永久发布页面还会提供带版本号的安装包，例如 `xui-deploy-assistant-1panel-3.0.6.zip`。固定文件名始终指向最近一次构建成功的版本，适合收藏和后续更新。

## 一、运行环境要求

### 支持环境

- 推荐使用最新稳定版 1Panel。
- 使用 1Panel 支持的 Linux 服务器。
- 当前安装包仅支持 `linux/amd64`，也就是常见的 `x86_64` 服务器；暂不支持 ARM64。
- Docker 和 Docker Compose 由 1Panel 安装和管理，无需另外部署。
- 容器内置 Node.js 20 运行环境，宿主机无需安装 Node.js、npm 或 PM2。

### 建议配置

```text
CPU：最低 1 核
内存：最低 512 MB，建议 1 GB 或以上
磁盘：至少预留 2 GB，并根据业务数据和备份数量增加空间
系统架构：linux/amd64（x86_64）
```

上面的资源只计算搭建助手本身。若同一台服务器还运行网站、数据库或其他容器，应额外预留资源。

## 二、网络、端口与防火墙要求

### 安装服务器需要访问的地址

- 服务器需要通过 HTTPS `443/tcp` 访问 `ghcr.io`，用于拉取公开容器镜像。
- 如果直接在服务器内下载安装包，还需要能够访问 `github.com` 和 GitHub Releases。
- 镜像为公开镜像，正常安装不需要 GitHub 用户名或访问令牌。

### 用户访问端口

- 安装向导中的默认 Web 端口是 `1888`，也可以换成其他未占用端口。
- 使用 `http://服务器IP:1888` 直接访问时，需要同时在云厂商安全组和服务器防火墙中放行该端口。
- 使用域名反向代理时，公网通常只需放行 `80/tcp` 和 `443/tcp`，应用端口无需对公网开放。
- 云厂商安全组和 Linux 防火墙是两层限制，只配置其中一处仍可能无法访问。

### 连接客户服务器所需网络

搭建助手需要从当前 1Panel 服务器主动连接要管理的客户 VPS，因此还应确认：

- 当前服务器可以访问客户 VPS 的 SSH 端口，默认是 `22/tcp`，使用自定义 SSH 端口时以实际端口为准。
- 当前服务器可以访问任务需要使用的目标面板端口或 API 端口。
- 客户 VPS 的安全组和防火墙已允许来自当前 1Panel 服务器公网出口 IP 的连接。
- SSH 地址、端口、账号或密钥填写正确。

如果应用页面可以正常打开，但搭建任务连接失败，通常应先排查这一段网络，而不是重新安装容器。

## 三、通过 1Panel 网页安装

先下载 `xui-deploy-assistant-1panel.zip`，然后根据当前 1Panel 版本选择下面一种方式。两种方式都不需要执行终端命令。

### 方式 A：使用本地应用导入功能

部分 1Panel 版本提供本地应用上传或 ZIP 导入入口：

1. 登录 1Panel，进入“应用商店”。
2. 打开“本地应用”或“应用导入”页面。
3. 上传 `xui-deploy-assistant-1panel.zip` 并完成导入。
4. 刷新本地应用列表。
5. 找到“X-UI 搭建助手”，点击“安装”。

如果当前界面没有 ZIP 导入入口，请使用方式 B。

### 方式 B：使用 1Panel 文件管理器

1. 登录 1Panel，进入“文件”管理器。
2. 打开本地应用目录：`/opt/1panel/resource/apps/local/`。
3. 将 `xui-deploy-assistant-1panel.zip` 上传到该目录。
4. 使用文件管理器的解压功能，将 ZIP 解压到当前目录。
5. 返回“应用商店”的“本地应用”页面，点击更新应用列表或刷新页面。
6. 找到“X-UI 搭建助手”，点击“安装”。

解压后的目录结构必须是：

```text
/opt/1panel/resource/apps/local/xui-deploy-assistant/
  data.yml
  logo.png
  README.md
  README_en.md
  3.0.6/
    data.yml
    docker-compose.yml
```

版本目录名称会随版本更新。不要解压成下面这种重复目录，否则 1Panel 无法识别应用：

```text
/opt/1panel/resource/apps/local/xui-deploy-assistant/xui-deploy-assistant/
```

如果 1Panel 的安装目录不是默认的 `/opt/1panel`，请在实际的 1Panel 安装目录下找到 `resource/apps/local/`，并保持相同的应用目录结构。

## 四、安装向导参数

安装时只需要设置“Web 端口”：

- 默认值为 `1888`。
- 端口必须在 `1-65535` 范围内，并且没有被其他网站、应用或容器占用。
- 该端口是服务器对外映射端口。容器内部端口固定为 `1888`，不需要修改。
- 如果设置为 `1888`，安装完成后的直接访问地址是 `http://服务器IP:1888`。
- 如果设置为其他端口，例如 `1900`，访问地址就是 `http://服务器IP:1900`。

确认参数后点击安装，等待 1Panel 拉取镜像、创建持久化目录并启动容器。容器显示“已启动”或“健康”后再打开访问地址。

## 五、首次运行与业务配置

首次访问一个全新数据目录时，系统会显示管理员初始化页面。按照页面提示创建第一个管理员账号即可，不需要在服务器上执行初始化命令。

建议按以下顺序完成正式使用前的配置：

1. 创建管理员并登录管理后台。
2. 设置站点名称、官网内容、联系方式和客服二维码。
3. 根据运营方式设置用户注册开关。
4. 配置 SMTP；需要邮箱验证或邮件通知时，再启用相关功能。
5. 配置实际使用的支付渠道，并完成支付回调测试。
6. 创建套餐，设置价格、有效期、面板次数、节点次数以及是否在官网展示。
7. 使用测试 VPS 验证 SSH、目标面板端口和节点任务的连通性。
8. 正式开放前绑定域名并启用 HTTPS。

如果首次打开直接显示登录页，通常表示当前安装复用了一个已经存在管理员的 `data` 目录。

## 六、绑定域名与 HTTPS

正式环境建议使用域名和 HTTPS：

1. 在 1Panel 中进入“网站”，创建一个反向代理网站。
2. 填写要使用的域名。
3. 将代理地址设置为 `http://127.0.0.1:Web端口`，例如 `http://127.0.0.1:1888`。
4. 确认域名 DNS 已解析到当前服务器。
5. 在网站的 HTTPS 设置中申请或上传证书。
6. 启用 HTTPS；确认访问正常后可开启强制 HTTPS。

应用会识别反向代理传入的 HTTPS 协议头，并为会话使用安全 Cookie。域名访问正常后，如果没有直接通过 IP 加端口访问的需求，可以在云安全组和防火墙中关闭 Web 应用端口的公网访问，只保留 `80` 和 `443`。

## 七、数据目录与备份

应用将容器内的 `/var/lib/xui-assistant` 映射到当前 1Panel 应用实例的 `data` 持久化目录。这里保存：

- 管理员和用户账号
- 套餐、有效期、面板次数和节点次数
- 订单、支付、邮件和商业配置
- 搭建任务及执行记录
- 官网、客服和系统设置
- SQLite 数据库及其运行辅助文件
- 商业配置加密密钥

主要文件可能包括：

```text
data/app.db
data/app.db-wal
data/app.db-shm
data/app.db.key
```

备份时必须备份整个 `data` 目录，不能只保存 `app.db`。`app.db.key` 是商业配置解密所需的密钥，必须与数据库一起保留；丢失密钥后，原数据库中的加密配置可能无法恢复。

建议在 1Panel 中停止应用后再执行完整备份，避免备份过程中数据库仍在写入。删除应用、重装、更新或迁移前，都应先确认完整备份可以找到。若 1Panel 询问是否同时删除应用数据，不要在未备份时选择删除。

## 八、更新应用

每次 `main` 分支远端构建成功后，自动更新以下内容：

- 永久安装包 `xui-deploy-assistant-1panel.zip`
- 当前版本安装包，例如 `xui-deploy-assistant-1panel-3.0.6.zip`
- 固定版本镜像，例如 `ghcr.io/wstimin/sy-xuizs:3.0.6`
- 滚动镜像 `ghcr.io/wstimin/sy-xuizs:latest`
- `SHA256SUMS` 校验文件

已安装的实例不会在后台静默自动升级。更新时建议：

1. 在 1Panel 中停止应用并备份整个 `data` 目录。
2. 下载最新的固定文件名安装包。
3. 按安装方式 A 或 B 导入最新版本地应用包。
4. 在 1Panel 中刷新本地应用列表，确认出现新版本。
5. 使用 1Panel 的应用更新功能更新；如果当前版本不支持直接更新，则重新安装应用并复用原来的 `data` 目录。
6. 启动后确认容器状态为“健康”。
7. 访问 `/api/health` 并检查管理员登录、套餐、订单和系统设置是否正常。

更新过程中不要删除或覆盖原实例的 `data` 目录。

## 九、迁移与卸载

迁移到另一台 1Panel 服务器时：

1. 停止旧服务器上的应用。
2. 备份并下载完整的 `data` 目录。
3. 在新服务器安装同版本或更新版本的 1Panel 专属包。
4. 停止新应用，将备份恢复到新实例对应的 `data` 目录。
5. 通过 1Panel 文件管理器确认目录和文件已完整恢复。
6. 启动应用，检查健康状态、管理员登录和历史数据。
7. 最后迁移域名解析、证书和防火墙规则。

卸载前同样应备份整个 `data` 目录。只有确认以后不再使用并且备份有效时，才同时删除应用数据。

## 十、运行信息

```text
应用名称：X-UI 搭建助手
容器运行时：Node.js 20（镜像内置）
容器内部端口：1888
默认公开端口：1888
健康检查路径：/api/health
容器持久化目录：/var/lib/xui-assistant
数据库文件：/var/lib/xui-assistant/app.db
容器镜像：ghcr.io/wstimin/sy-xuizs:3.0.6
容器网络：1panel-network
重启策略：always
支持架构：linux/amd64
```

## 十一、常见问题

### 本地应用列表中找不到 X-UI 搭建助手

检查 ZIP 是否解压到 1Panel 的 `resource/apps/local/` 目录，目录名称是否为 `xui-deploy-assistant`，以及是否出现了两层同名目录。确认结构正确后，在本地应用页面更新应用列表或刷新页面。

### 镜像拉取失败

检查服务器 DNS、出站网络以及到 `ghcr.io:443` 的连接。镜像是公开的，不需要填写 GitHub 登录信息。

### 安装提示端口被占用

返回安装向导，换用一个未被网站、容器或其他程序使用的 Web 端口。修改的是公开端口，容器内部端口仍为 `1888`。

### 容器启动但一直不健康

在 1Panel 的应用或容器页面查看日志。重点检查数据目录是否可写、数据库是否完整，以及容器是否持续重启。健康检查地址为：

```text
http://127.0.0.1:Web端口/api/health
```

### 服务器 IP 加端口无法访问

确认容器已启动，并分别检查 1Panel 防火墙、Linux 防火墙和云厂商安全组。直接访问需要放行安装时设置的 Web 端口。

### 首次打开没有进入管理员初始化

当前 `data` 目录中已经存在管理员数据时，系统会直接显示登录页。这通常表示安装时复用了旧数据，不代表初始化功能失效。

### 更新或迁移后数据不见了

检查新容器是否挂载了原来的完整 `data` 目录。业务数据不保存在镜像内，只替换镜像但没有恢复持久化目录时，应用会使用新的空数据库。

### 页面正常，但远程搭建任务连接失败

检查客户 VPS 的 SSH 地址、端口、账号、密码或密钥，并确认客户 VPS 的安全组和防火墙允许当前 1Panel 服务器的公网出口 IP。目标面板或 API 使用的端口也必须允许访问。

### 如何确认应用服务正常

在浏览器访问：

```text
http://服务器IP:Web端口/api/health
```

使用域名时访问：

```text
https://你的域名/api/health
```

返回内容包含 `"status":"ok"`，表示应用服务已经正常响应。
